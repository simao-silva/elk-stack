filebeat:
  inputs:
    -
      paths:
        - /var/log/auth.log
      input_type: log
      multiline.pattern: '^[0-9]{4}-[0-9]{2}-[0-9]{2}'
      multiline.negate: true
      multiline.match: after

output:
  logstash:
    hosts: ["172.168.1.78:5044"]

logging.level: debug
